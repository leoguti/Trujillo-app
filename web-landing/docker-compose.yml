services:
  web-landing:
    container_name: web-landing
    image: nginx:alpine
    volumes:
      - ./html:/usr/share/nginx/html:ro
    restart: unless-stopped

networks:
  default:
    name: trufi-server
